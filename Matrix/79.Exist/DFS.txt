时间：O（M*N*3^L）
check函数，除第一次外4次，其他每次递归3个方向，字符串长L，因此复杂度上限是O（3^L）。但是实际上会远小于此。
主函数，逐点check，MN个点。
空间：O(M*N)
最坏情况：在check某一点时，visited集合的元素个数是board上的每一个点。而堆栈的最大深度是O（min(L,M*N)）。

visited用集合，而不用矩阵，这样节省空间。

DFS：递归函数先写特殊情况（1.检查的点和下一个字母不等2.检查完毕），然后把当前点存入visited，然后检查相邻的点（递归结果存起来），最后删除当前点，返回结果。
